<Window x:Class="AskAny.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AskAny 设置"
        Width="860"
        Height="700"
        MinWidth="760"
        MinHeight="620"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True">
    <Border Margin="10" Background="White" BorderBrush="#DADAE0" BorderThickness="1" CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="26" ShadowDepth="4" Opacity="0.16" Color="#202124" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="72" />
                <RowDefinition Height="1" />
                <RowDefinition Height="*" />
                <RowDefinition Height="66" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="18,8,10,8" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Width="34" Height="34" HorizontalAlignment="Left" VerticalAlignment="Center" Source="Assets/AskAny-transparent.png" />
                <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource InkBrush}" Text="设置" />
                <Button Grid.Column="2" Style="{StaticResource IconButton}" ToolTip="关闭" Click="CloseButton_Click">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Text="&#xE711;" />
                </Button>
            </Grid>
            <Border Grid.Row="1" Background="{StaticResource LineBrush}" />

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#FAFAFB">
                    <Grid Margin="16,18,14,16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="提供商" />
                        <ListBox x:Name="ProviderList"
                                 Grid.Row="1"
                                 Margin="0,10,0,0"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 SelectionChanged="ProviderList_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="11,10" />
                                    <Setter Property="Margin" Value="0,2" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ProviderRow" Padding="{TemplateBinding Padding}" Background="Transparent" CornerRadius="6">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ProviderRow" Property="Background" Value="#F0F2F5" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ProviderRow" Property="Background" Value="{StaticResource AccentSoftBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource InkBrush}" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <StackPanel Grid.Row="2" Margin="0,14,0,0">
                            <TextBlock Style="{StaticResource SectionTitle}" Text="新增提供商" />
                            <ComboBox x:Name="PresetComboBox" Margin="0,8,0,0" Height="36" DisplayMemberPath="Name" Background="White" Style="{StaticResource ComboField}" />
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button x:Name="AddProviderButton" Style="{StaticResource SecondaryButton}" Padding="12,8" Content="添加" Click="AddProviderButton_Click" />
                                <Button x:Name="DeleteProviderButton" Grid.Column="1" Margin="8,0,0,0" Style="{StaticResource SecondaryButton}" Padding="12,8" Content="删除" Click="DeleteProviderButton_Click" />
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource LineBrush}" />

                <TabControl x:Name="SettingsTabs" Grid.Column="2" Margin="12,12,16,8" Background="Transparent" BorderThickness="0">
                    <TabItem Header="模型连接">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20,16,20,20">
                                <Border Style="{StaticResource CardBorder}" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="连接信息" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="提供商名称" />
                                        <TextBox x:Name="ProviderNameBox" Style="{StaticResource FormField}" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="API 协议" />
                                        <ComboBox x:Name="ProtocolComboBox" Height="36" DisplayMemberPath="Name" Background="White" Style="{StaticResource ComboField}" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="接口地址" />
                                        <TextBox x:Name="BaseUriBox" Style="{StaticResource FormField}" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="API Key" />
                                        <PasswordBox x:Name="ApiKeyBox" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorder}" Margin="0,14,0,0" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="可用模型" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="模型列表（每行一个）" />
                                        <TextBox x:Name="ModelsBox"
                                                 Height="110"
                                                 Padding="10,8"
                                                 AcceptsReturn="True"
                                                 TextWrapping="NoWrap"
                                                 VerticalContentAlignment="Top"
                                                 VerticalScrollBarVisibility="Auto"
                                                 FontFamily="Cascadia Mono, Consolas"
                                                 FontSize="13"
                                                 Style="{StaticResource FormField}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="推理与搜索">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20,16,20,20">
                                <Border Style="{StaticResource CardBorder}" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="推理控制" />
                                        <CheckBox x:Name="ReasoningCheck" Margin="0,10,0,0" FontSize="13" Foreground="{StaticResource InkBrush}" Content="启用推理强度控制" Checked="ReasoningCheck_Changed" Unchecked="ReasoningCheck_Changed" />
                                        <Grid Margin="0,10,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="150" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource MutedBrush}" Text="推理强度" />
                                            <ComboBox x:Name="ReasoningEffortComboBox" Grid.Column="1" Margin="14,0,0,0" Background="White" Style="{StaticResource ComboField}" />
                                        </Grid>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorder}" Margin="0,14,0,0" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="联网搜索" />
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="Tavily API Key" />
                                        <PasswordBox x:Name="TavilyKeyBox" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="窗口偏好">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20,16,20,20">
                                <Border Style="{StaticResource CardBorder}" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="窗口行为" />
                                        <CheckBox x:Name="TopMostCheck" Margin="0,8,0,0" FontSize="13" Foreground="{StaticResource InkBrush}" Content="窗口始终置顶" />
                                        <CheckBox x:Name="HideWhenDeactivatedCheck" Margin="0,2,0,0" FontSize="13" Foreground="{StaticResource InkBrush}" Content="失去焦点后自动隐藏" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorder}" Margin="0,14,0,0" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="启动" />
                                        <CheckBox x:Name="StartWithWindowsCheck" Margin="0,8,0,0" FontSize="13" Foreground="{StaticResource InkBrush}" Content="开机时自动启动" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorder}" Margin="0,14,0,0" Padding="16,14">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitle}" Text="输入" />
                                        <CheckBox x:Name="AutoFillSelectionCheck" Margin="0,8,0,0" FontSize="13" Foreground="{StaticResource InkBrush}" Content="自动填入选中文字" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Grid>

            <Grid Grid.Row="3" Margin="18,9">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="SettingsStatusText" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" />
                <Button x:Name="TestButton" Grid.Column="1" Margin="0,0,9,0" Style="{StaticResource SecondaryButton}" Content="测试连接" Click="TestButton_Click" />
                <Button Grid.Column="2" MinWidth="84" Margin="0,0,9,0" Style="{StaticResource SecondaryButton}" Content="取消" Click="CloseButton_Click" />
                <Button x:Name="SaveButton" Grid.Column="3" MinWidth="90" Style="{StaticResource PrimaryButton}" Content="保存" Click="SaveButton_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>